<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
        <link rel="stylesheet" href="css/produits.css">
        <link rel="stylesheet" href="css/login.css">
        {% endblock %}
        {% block head_css %}
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
        {% endblock %}
        {% block head_js %}
            <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js"></script>
            <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
            <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>
        {% endblock %}

    </head>

    <body>
    {% block navbar %}

        <nav class="navbar navbar-expand-lg navbar-light bg-transparent" style="font-family: cursive">

            <a href="{{ path("accueil") }}" ><img id="logoIndex" src="{{ asset('images/f.gif') }}" style="inline-size: 42px; margin-right: 10px;"></a>
            <a class="navbar-brand" href="{{ path("accueil") }}" style="color: #343a40;">Shop 404

            </a>


            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
                <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path("accueil") }}" style="color: #343a40;">Accueil<span class="sr-only">(current)</span></a>
                    </li>

                     <li class="nav-item active">
                        <a class="nav-link" href="{{ path("produits_index") }}" style="color: #343a40;">Produits <span class="sr-only">(current)</span></a>
                    </li>
                </ul>
                <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                    <li class="nav-item active">
{#                        <a class="nav-link" href="{{ path("categorie") }}">Catégorie <span class="sr-only">(current)</span></a>#}
                    </li>
                </ul>
                <form class="form-inline my-2 my-lg-0">
                    {% if not is_granted('ROLE_USER') %}
                        <a href="{{ path("app_login") }}" class="btn btn-primary" style="background-color: darkslategrey">Se connecter</a>
                    {% else %}
                        <a href="{{ path("app_logout") }}" class="btn btn-warning">Se déconnecter</a>
                    {% endif %}
                </form>

            </div>

        </nav>



    {% endblock %}
    {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    {% block footer %}
        <footer style="background-color: #343a40; color: white; text-align: center; font-family: cursive; padding: 2rem">
            © Tous droits réservés.
            <a href="https://github.com/zozo404/boutique"><p style="text-decoration: none; color: white;">zozo_404</p></a>

        </footer>
    {% endblock %}
    </body>

</html>
